# Android emulator: use 10.0.2.2 to reach the host machine (this is the default in api.ts).
# Physical device on the same Wi-Fi: use the host machine's LAN IP, e.g. http://192.168.1.23:4000
EXPO_PUBLIC_API_URL=http://10.0.2.2:4000
